# Netlify redirects

# Bot/Crawler redirection for social sharing previews
/confessions/:id/:slug /?path=/confessions/:id/:slug 200! Function=/meta-tag-renderer

# Fallback for all other routes to the main app
/* /index.html 200